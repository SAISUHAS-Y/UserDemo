spring.application.name=UserDemo
server.port = 8080

#Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/user_database
spring.datasource.username=root
spring.datasource.password=saisuhas@421
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Jpa Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Show Spring Web MVC request logs (dispatcher servlet, handler mapping)
logging.level.org.springframework.web=DEBUG

# Show Hibernate SQL queries (optional, for DB debugging)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# General Spring Boot startup logs and context
logging.level.org.springframework.boot=INFO
